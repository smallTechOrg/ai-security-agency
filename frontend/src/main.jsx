import React,{useEffect,useState}from'react';
import{createRoot}from'react-dom/client';
import{Shield,Activity,FileText,CheckCircle,AlertTriangle,DollarSign,Brain,LockKeyhole}from'lucide-react';
import'./style.css';
const API=import.meta.env.VITE_API_BASE_URL||'http://127.0.0.1:8011';

function App(){
  const[health,setHealth]=useState(null),[dash,setDash]=useState(null),[admin,setAdmin]=useState(null),[domains,setDomains]=useState(null),[auditLog,setAuditLog]=useState(null),[billing,setBilling]=useState(null),[schedules,setSchedules]=useState(null),[tickets,setTickets]=useState(null),[summary,setSummary]=useState(null),[readiness,setReadiness]=useState(null),[users,setUsers]=useState(null),[costGov,setCostGov]=useState(null),[intelModels,setIntelModels]=useState(null),[intelMode,setIntelMode]=useState('deterministic'),[url,setUrl]=useState('https://example.com'),[tier,setTier]=useState('free'),[payment,setPayment]=useState(''),[run,setRun]=useState(null),[report,setReport]=useState(null),[timeline,setTimeline]=useState(null),[tasks,setTasks]=useState(null),[intel,setIntel]=useState(null),[enterprise,setEnterprise]=useState(null),[busy,setBusy]=useState(false),[error,setError]=useState('');
  async function api(path,options){const r=await fetch(API+path,options),j=await r.json().catch(()=>({detail:r.statusText}));if(!r.ok)throw new Error(j.detail||`HTTP ${r.status}`);return j}
  async function load(){try{setHealth(await api('/health'));setDash(await api('/api/dashboard'));setAdmin(await api('/api/admin/domain-queue'));setDomains(await api('/api/admin/domains'));setAuditLog(await api('/api/admin/audit-log'));setBilling(await api('/api/billing/plans'));setSchedules(await api('/api/admin/schedules'));setTickets(await api('/api/remediation-tickets'));setSummary(await api('/api/program/summary'));setReadiness(await api('/api/program/readiness'));setUsers(await api('/api/admin/users'));setIntelModels(await api('/api/intelligence/models'));setIntelMode((await api('/api/intelligence/models')).current)}catch{}}
  useEffect(()=>{load()},[]);
  function clearReport(){setReport(null);setTimeline(null);setTasks(null);setIntel(null);setEnterprise(null)}
  async function buyDetailed(){setBusy(true);setError('');try{const p=await api('/api/payments/intent',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({target_url:url,scan_tier:'detailed'})});setPayment(p.payment_reference);setTier('detailed')}catch(e){setError(e.message)}finally{setBusy(false)}}
  async function start(){
    setBusy(true);setError('');clearReport();
    try{const j=await api('/api/bootstrap',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({target_url:url,client_name:'Vanguard Client',workspace_name:'Zer0 Security Program',budget_usd:tier==='detailed'?49:2.0,scan_tier:tier,payment_reference:payment})});setRun(j);await load()}
    catch(e){setError(e.message)}finally{setBusy(false)}
  }
  async function openRun(r){
    const id=r.run_id||r.id;
    setBusy(true);setError('');setRun({run_id:id,workspace_id:r.workspace_id,asset_id:r.asset_id||0,status:r.status,stage:r.stage,progress:r.progress,app_model:r.app_model||{}});clearReport();
    try{if(['awaiting_approval','payment_required','queued'].includes(r.status)){if(r.workspace_id)setCostGov(await api(`/api/workspaces/${r.workspace_id}/cost-governor?run_id=${id}`));return;}const [rep,tl,tk,ai,ent,gov]=await Promise.all([api(`/api/runs/${id}/report`),api(`/api/runs/${id}/timeline`),api(`/api/runs/${id}/tasks`),api(`/api/runs/${id}/intelligence`),api(`/api/workspaces/${r.workspace_id}/enterprise`),api(`/api/workspaces/${r.workspace_id}/cost-governor?run_id=${id}`)]);setReport(rep);setTimeline(tl);setTasks(tk);setIntel(ai);setEnterprise(ent);setCostGov(gov)}
    catch(e){setError(e.message)}finally{setBusy(false)}
  }
  async function approve(){
    const active=run?.status==='awaiting_approval'?run:dash?.runs?.[0]?.status==='awaiting_approval'?dash.runs[0]:null;
    if(!active)return setError('Create a free audit or paid detailed scan first; admin approval unlocks testing.');
    const id=active.run_id||active.id;
    setBusy(true);setError('');
    try{await api(`/api/runs/${id}/approve`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({decided_by:'admin',reason:'Domain ownership and testing scope approved.'})});const executed=await api(`/api/runs/${id}/execute`,{method:'POST'});setRun(executed);const br=await api(`/api/runs/${id}/browser-recon`,{method:'POST'});setRun({...executed,status:br.status,stage:br.stage,progress:br.progress});await api(`/api/workspaces/${active.workspace_id}/enterprise-program`,{method:'POST'});await load();await openRun({...executed,status:br.status,stage:br.stage,progress:br.progress})}
    catch(e){setError(e.message)}finally{setBusy(false)}
  }
  async function approveQueueRun(x){
    setBusy(true);setError('');
    try{const approved=await api(`/api/admin/domain-queue/${x.run_id}/approve`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({decided_by:'admin',reason:'Domain owner/admin approved from Vanguard queue.'})});setRun(approved);await load()}
    catch(e){setError(e.message)}finally{setBusy(false)}
  }
  async function executeQueueRun(x){
    setBusy(true);setError('');
    try{const done=await api(`/api/admin/domain-queue/${x.run_id}/execute`,{method:'POST'});await load();await openRun({...done,id:done.run_id})}
    catch(e){setError(e.message)}finally{setBusy(false)}
  }
  async function revokeDomain(x){
    setBusy(true);setError('');
    try{await api(`/api/admin/domains/${x.asset_id}/revoke`,{method:'POST'});await load()}
    catch(e){setError(e.message)}finally{setBusy(false)}
  }
  async function enableSchedule(x){
    setBusy(true);setError('');
    try{await api(`/api/admin/schedules/${x.workspace_id}/enable`,{method:'POST'});await load()}
    catch(e){setError(e.message)}finally{setBusy(false)}
  }
  async function closeTicket(t){
    setBusy(true);setError('');
    try{await api(`/api/remediation-tickets/${t.id}/status`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({status:'closed'})});await load()}
    catch(e){setError(e.message)}finally{setBusy(false)}
  }
  async function pickIntelMode(e){const m=e.target.value;setIntelMode(m);try{await api('/api/intelligence/models',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({mode:m})})}catch(err){setError(err.message)}}
  const newestRun=dash?.runs?.[0],pendingRun=run?.status==='awaiting_approval'?run:(newestRun?.status==='awaiting_approval'?newestRun:null),current=run||newestRun,findings=report?.findings||dash?.findings||[],commerce=dash?.commerce||{};
  return <div><header><div><h1><Shield/> Zer0 — The Vanguard</h1><p>A security agent for your company.</p></div><div className="pill">OpenAI {health?.provider?.openai?'ready':'missing'} · Gemini {health?.provider?.gemini?'ready':'missing'}</div></header><main><section className="hero"><div><span className="eyebrow">Enterprise web exposure management</span><h2>Audit a public domain with approvals, budget controls, and evidence.</h2><p>Start with a free high-level posture check. Detailed scans are payment-gated and require backend admin domain approval before testing.</p><div className="pricing"><button className={tier==='free'?'selected':''} onClick={()=>setTier('free')}>Free audit<span>Headers, TLS, public evidence</span></button><button className={tier==='detailed'?'selected':''} onClick={buyDetailed}>Detailed scan · ${commerce.detailed_scan_price_usd||49}<span>Paid intent + admin domain approval</span></button></div><div className="row"><input value={url} onChange={e=>setUrl(e.target.value)}/><button disabled={busy} onClick={start}>{busy?'Working...':tier==='free'?'Start free audit':'Request detailed scan'}</button>{pendingRun&&<button disabled={busy} onClick={approve}>Admin approve domain + run</button>}</div><div className="row"><label className="modelpick">AI model <select value={intelMode} onChange={pickIntelMode}>{intelModels?.models?.map(m=><option key={m.id} value={m.id}>{m.label}</option>)}</select></label></div>{payment&&<div className="hint"><LockKeyhole size={16}/> Payment intent staged: <b>{payment}</b></div>}{error&&<div className="error">{error}</div>}<div className="hint">Previous runs are below. Billing supports stub checkout intents and a Stripe-ready webhook endpoint.</div></div><div className="status"><Activity/><b>{current?current.status:'ready'}</b><span>{current?current.stage:'awaiting authorized target'}</span><progress max="100" value={current?.progress||0}/></div></section><section className="grid"><Card icon={<DollarSign/>} title="Budget remaining" value={`$${costGov?.remaining_usd??summary?.commerce?.estimated_scan_revenue_usd??dash?.cost?.estimated_total_usd??0}`} sub={costGov?`${costGov.allowed?'Allowed':'Blocked'} · projected $${costGov.projected_run_cost_usd}`:'Cost governor ready'}/><Card icon={<CheckCircle/>} title="Approved domains" value={summary?.operations?.domains_approved??dash?.approvals?.length??0} sub="Admin-approved attack surface"/><Card icon={<AlertTriangle/>} title="Open tickets" value={summary?.risk?.open_remediation_tickets??findings.length} sub="Remediation workload"/><Card icon={<Brain/>} title="Readiness" value={`${readiness?.ready_score??0}%`} sub="Launch checklist"/></section>{report&&<Report report={report} intel={intel} enterprise={enterprise} tasks={tasks} timeline={timeline}/>}<section className="mission"><h2>Vanguard control plane</h2><p>Domain intake → payment tier → admin approval → safe audit execution → evidence timeline → executive report → remediation program.</p></section><section><h2>Operator console</h2><div className="cols"><RunList runs={dash?.runs} active={current} onOpen={openRun}/><DomainQueue items={admin?.items} onApprove={approveQueueRun} onExecute={executeQueueRun}/><DomainRegistry domains={domains?.domains} onRevoke={revokeDomain}/></div><ScheduleList schedules={schedules?.schedules} onEnable={enableSchedule}/><TeamList users={users?.users}/><TicketList tickets={tickets?.tickets} onClose={closeTicket}/><AuditLog events={auditLog?.events}/></section></main></div>
}
function Report({report,intel,enterprise,tasks,timeline}){return <section className="report"><h2><FileText/> Vanguard security report</h2><div className="actions"><a href={`${API}/api/runs/${report.run_id}/report.html`} target="_blank">Open HTML report</a><a href={`${API}/api/runs/${report.run_id}/evidence-bundle`} target="_blank">Download evidence JSON</a><a href={`${API}/api/client/reports/${report.run_id}`} target="_blank">Client-safe report</a><a href={`${API}/api/runs/${report.run_id}/attestation`} target="_blank">Scope attestation</a></div><div className="score">Security score <b>{report.security_score}</b>/100 · {report.certificate_status}</div><p>{report.executive_summary}</p><h3>Application model</h3><pre>{JSON.stringify(report.app_model,null,2)}</pre><h3>AI report intelligence</h3><pre>{JSON.stringify(intel,null,2)}</pre><h3>Enterprise program scaffold</h3><pre>{JSON.stringify(enterprise,null,2)}</pre><h3>Findings</h3>{report.findings.map((f,i)=><div className={'finding '+f.severity.toLowerCase()} key={i}><b>{f.severity}: {f.title}</b><p>{f.description}</p><small>{f.remediation} · {Object.values(f.compliance||{}).join(', ')}</small></div>)}<h3>Agent tasks</h3>{tasks?.tasks?.map((t,i)=><div className="log" key={i}>{t.status} · {t.module} · {t.summary}</div>)}<h3>Cost events</h3>{tasks?.costs?.map((c,i)=><div className="log" key={i}>${c.estimated_usd} · {c.provider} · {c.operation}</div>)}<h3>Evidence timeline</h3>{timeline?.logs?.map((l,i)=><div className="log" key={i}>{l.created_at} · {l.actor} · {l.action}</div>)}</section>}
function RunList({runs=[],active,onOpen}){return <div className="panel"><h3>Previous runs</h3>{(runs||[]).map(r=><button className={'runitem '+((active?.run_id||active?.id)===r.id?'selected':'')} key={r.id} onClick={()=>onOpen(r)}><b>Run #{r.id}</b><span>{r.status} · {r.stage}</span><small>{r.app_model?.scan_tier||'legacy'} · ${r.cost_estimate_usd} · workspace {r.workspace_id}</small></button>)}</div>}
function DomainQueue({items=[],onApprove,onExecute}){return <div className="panel"><h3>Admin domain queue</h3>{(items||[]).slice(0,12).map(x=><div className="approval" key={x.approval_id}><b>{x.status}</b><span>{x.domain||'domain pending'}</span><small>{x.scan_tier} · {x.payment_status} · run #{x.run_id}</small>{x.status==='pending'&&<button onClick={()=>onApprove(x)}>Approve domain</button>}{x.status==='approved'&&x.run_status==='queued'&&<button onClick={()=>onExecute(x)}>Run scan</button>}</div>)}</div>}
function DomainRegistry({domains=[],onRevoke}){return <div className="panel"><h3>Domain registry</h3>{(domains||[]).slice(0,12).map(d=><div className="approval" key={d.asset_id}><b>{d.authorized?'approved':'revoked'}</b><span>{d.url}</span><small>asset #{d.asset_id} · workspace {d.workspace_id}</small>{d.authorized&&<button onClick={()=>onRevoke(d)}>Revoke</button>}</div>)}</div>}
function AuditLog({events=[]}){return <div className="panel audit"><h3>Immutable audit log</h3>{(events||[]).slice(0,16).map(e=><div className="log" key={e.id}>{e.created_at} · {e.actor} · {e.action} · run {e.run_id}</div>)}</div>}
function ScheduleList({schedules=[],onEnable}){return <div className="panel audit"><h3>Recurring schedules</h3>{(schedules||[]).slice(0,12).map(s=><div className="approval" key={s.id}><b>{s.status}</b><span>workspace {s.workspace_id} · asset {s.asset_id}</span><small>{s.cadence} · {s.next_run_note}</small>{s.status!=='active'&&<button onClick={()=>onEnable(s)}>Enable weekly</button>}</div>)}</div>}
function TeamList({users=[]}){return <div className="panel audit"><h3>Team roles</h3>{(users||[]).slice(0,12).map(u=><div className="approval" key={u.id}><b>{u.role}</b><span>{u.email}</span><small>workspace {u.workspace_id}</small></div>)}</div>}
function TicketList({tickets=[],onClose}){return <div className="panel audit"><h3>Remediation tickets</h3>{(tickets||[]).slice(0,12).map(t=><div className="approval" key={t.id}><b>{t.status}</b><span>{t.severity}: {t.title}</span><small>owner {t.owner} · finding #{t.finding_id}</small>{t.status!=='closed'&&<button onClick={()=>onClose(t)}>Close ticket</button>}</div>)}</div>}
function Card({icon,title,value,sub}){return <div className="card">{icon}<h3>{title}</h3><b>{value}</b><p>{sub}</p></div>}
function Panel({title,data=[]}){return <div className="panel"><h3>{title}</h3><pre>{JSON.stringify(data||[],null,2)}</pre></div>}
createRoot(document.getElementById('root')).render(<App/>);
